{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window. Custom app commands are allowed by default; only core window controls need to be granted here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
